import React from 'react'

function JSXRules() {
  return (
    <div>
        <h1>JSX Rules</h1>
        <p>JSX must return a single parent element</p>
        <p>JSX elements must be properly closed</p>
        <p className=''>JSX attributes are written using camelCase (e.g. className instade of class)</p>
    </div>
  )
}

export default JSXRules